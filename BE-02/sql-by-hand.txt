listening on http://localhost:3000
$ node server.js &                       # server running, NOT restarted at any point below
$ curl -s localhost:3000/tasks
[{"id":1,"title":"Read the assignment","done":true},{"id":2,"title":"Connect the CRUD to SQLite","done":false},{"id":3,"title":"Prove the data survives a restart","done":false}]

$ sqlite3 tasks.db                       # same file, opened by hand next to the running API
sqlite> SELECT * FROM tasks;
1|Read the assignment|1
2|Connect the CRUD to SQLite|0
3|Prove the data survives a restart|0
sqlite> SELECT * FROM tasks WHERE done = 1;
1|Read the assignment|1
sqlite> SELECT COUNT(*) FROM tasks;
3
sqlite> UPDATE tasks SET done = 1;
sqlite> SELECT * FROM tasks;
1|Read the assignment|1
2|Connect the CRUD to SQLite|1
3|Prove the data survives a restart|1
sqlite> DELETE FROM tasks WHERE done = 1;
sqlite> SELECT COUNT(*) FROM tasks;
0
sqlite> INSERT INTO tasks (title, done) VALUES ('added by hand in SQL', 0);

$ curl -s localhost:3000/tasks           # no restart, no sync: the API already sees it
[{"id":1,"title":"added by hand in SQL","done":false}]
